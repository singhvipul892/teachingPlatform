package com.maths.teacher.app.ui.theme

import androidx.compose.ui.graphics.Color

// ============ Primary tokens ============
// Primary Blue: app header, bottom nav, primary buttons, section titles
val PrimaryBlue = Color(0xFF0B2C5F)
val PrimaryBlueDark = Color(0xFF081E42)
val PrimaryBlueLight = Color(0xFF0F3A7A)

// Accent Orange (Saffron): CTAs, active states, progress, highlights
val AccentSaffron = Color(0xFFF47A2A)
val AccentSaffronDark = Color(0xFFC56222)
val AccentSaffronLight = Color(0xFFF7944D)

// ============ Secondary tokens ============
// Purple: video categories, test series banners, feature sections
val CategoryPurple = Color(0xFF4B4ECF)
val CategoryPurpleDark = Color(0xFF3C3EA6)
val CategoryPurpleLight = Color(0xFF6B6ED9)

// Teal: secondary actions, icons, links, info highlights
val SecondaryTeal = Color(0xFF2FB7B0)
val SecondaryTealDark = Color(0xFF26928D)
val SecondaryTealLight = Color(0xFF4FC9C2)

// ============ Background tokens ============
// Main: all main content screens
val BackgroundMain = Color(0xFFFFFFFF)
val BackgroundMainDark = Color(0xFF121212)

// Card / Section: cards, lists, section separation
val BackgroundCard = Color(0xFFF3F4F6)
val BackgroundCardDark = Color(0xFF1E1E1E)

// ============ Text tokens ============
// Primary: body text, questions, explanations
val TextPrimary = Color(0xFF1F2937)
val TextPrimaryDark = Color(0xFFE5E7EB)

// Secondary: subtitles, metadata, timestamps
val TextSecondary = Color(0xFF6B7280)
val TextSecondaryDark = Color(0xFF9CA3AF)

// ============ Semantic (on-color) ============
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnTertiary = Color(0xFFFFFFFF)
val OnBackground = TextPrimary
val OnBackgroundDark = TextPrimaryDark
val OnSurface = TextPrimary
val OnSurfaceDark = TextPrimaryDark
val OnSurfaceVariant = TextSecondary
val OnSurfaceVariantDark = TextSecondaryDark

// ============ Error ============
val Error = Color(0xFFBA1A1A)
val ErrorDark = Color(0xFF93000A)
val OnError = Color(0xFFFFFFFF)

// Outline (subtle borders)
val Outline = Color(0xFFD1D5DB)
val OutlineDark = Color(0xFF4B5563)
